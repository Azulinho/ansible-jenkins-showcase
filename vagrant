#
#	Ansible inventory file for: vagrant
#

[jenkins_servers]
jenkins group=jenkins_servers deploy_env=vagrant

[zabbix_servers]
zabbix group=zabbix_servers deploy_env=vagrant

[vagrant:children]
jenkins_servers
zabbix_servers
